language: python
python:
  - "3.10.*"  # Use the development version of Python 3.10

env:
  - DJANGO_SETTINGS_MODULE="mysite.settings"

install:
  - pip install -r requirements.txt

script:
  - python manage.py migrate
  - python manage.py test
